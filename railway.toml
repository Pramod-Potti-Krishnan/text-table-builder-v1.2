[build]
builder = "NIXPACKS"

[deploy]
# Uses SERVICE_MODE env var: "web" (default) or "worker"
startCommand = "python main.py"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
